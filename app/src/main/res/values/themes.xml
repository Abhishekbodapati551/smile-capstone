<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SmileApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/child_blue</item>
        <item name="colorSecondary">@color/child_pink</item>
        <item name="colorTertiary">@color/child_purple</item>
        <item name="android:statusBarColor">@color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="Theme.SmileApp" parent="Base.Theme.SmileApp" />
</resources>